spring.application.name=backend

# PostgreSQL Configuration (Supabase)
# Prefer a full JDBC URL when provided; otherwise build it from discrete Supabase vars.
spring.datasource.url=jdbc:postgresql://aws-1-eu-west-1.pooler.supabase.com:5432/postgres?sslmode=require&user=postgres.svnduqdzxeuqfreyplhx&password=puigcastellar
spring.datasource.username=postgres.svnduqdzxeuqfreyplhx
spring.datasource.password=puigcastellar
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080
